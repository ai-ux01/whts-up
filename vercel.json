{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm install --filter web...",
  "buildCommand": "pnpm --filter @whats-up/shared build && pnpm --filter web build",
  "outputDirectory": "apps/web/.next"
}
